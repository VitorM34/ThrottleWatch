<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ThrottleWatch.Dashboard</RootNamespace>
    <AssemblyName>ThrottleWatch.Dashboard</AssemblyName>
    <UserSecretsId>throttlewatch-dashboard-dev</UserSecretsId>
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <!-- .NET 10: ensure blazor.web.js is published (required for Docker / cross-build). -->
    <RequiresAspNetWebAssets>true</RequiresAspNetWebAssets>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Blazor-ApexCharts" Version="6.1.0" />
  </ItemGroup>

</Project>
